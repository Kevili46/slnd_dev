import { OmniClickDirective } from './omni-click.directive';

describe('OmniClickDirective', () => {
  it('should create an instance', () => {
    const directive = new OmniClickDirective();
    expect(directive).toBeTruthy();
  });
});
